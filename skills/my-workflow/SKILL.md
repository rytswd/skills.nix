---
name: my-workflow
description: >-
  The user's git, review, and deployment workflow on this machine.
  Load before any version-control action (commit / push / merge /
  rebase / force-push), opening or reviewing a PR, or running a
  deploy.
metadata:
  version: "0.1"
  author: skills-nix
  template: "true"
---

# My Workflow

> **Template skill.** This file ships empty by design. Replace each
> section below with concrete, short facts about *your* workflow.
> Delete sections you don't care about; empty sections waste context
> tokens and dilute the description's trigger.
>
> See `programs.agent-skills.localSkills` in skills.nix for how to
> mount your own copy.

## Branching / commits
<!-- e.g. "trunk-based; rebase on top of main; no merge commits in feature branches." -->

## PR opening / review
<!-- e.g. "Open as draft; self-review first; tag reviewers only after CI green." -->

## Merging / rebasing / force-push
<!-- e.g. "Squash-and-merge via GitHub; force-push allowed only on own branches." -->

## Releases / deploys
<!-- e.g. "Tag with `vX.Y.Z`; deploy via `just release`; no manual steps on prod." -->
